import { ref } from "vue";

export const windowstatus = ref({
    logginscreen: false,
    registerscreen: true,
    accountPage: false,
    recentProjectsPage: false,
    projectDetailsPage: true,
    createProjectPage: false,

});